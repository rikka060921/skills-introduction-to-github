# 年龄排序算法分析

## 问题描述

如何根据年龄给 100 万用户排序？假设年龄的范围最小 1 岁，最大不超过 120 岁。

## 解决方案

本程序使用 C 语言实现了三种不同的排序算法，并对它们的执行效率进行分析和比较。

## 实现的三种排序算法

### 1. 计数排序 (Counting Sort)

**算法原理：**
- 统计每个年龄值出现的次数
- 计算每个年龄值在排序后数组中的位置
- 将用户按年龄顺序放入正确位置

**时间复杂度：** O(n + k)，其中 n 是用户数，k 是年龄范围（120）  
**空间复杂度：** O(n + k)  
**稳定性：** 稳定排序

**优点：**
- 对于有限范围的整数排序非常高效
- 在本场景中性能最优
- 线性时间复杂度

**缺点：**
- 需要额外的空间
- 只适用于整数且范围不太大的情况

### 2. 快速排序 (Quick Sort)

**算法原理：**
- 选择一个基准值（pivot）
- 将数组分为小于和大于基准值的两部分
- 递归地对两部分进行排序

**时间复杂度：** 平均 O(n log n)，最坏 O(n²)  
**空间复杂度：** O(log n)（递归栈）  
**稳定性：** 不稳定排序

**优点：**
- 通用排序算法，适用范围广
- 平均性能优秀
- 原地排序，空间效率高

**缺点：**
- 最坏情况性能较差
- 不稳定
- 对于本场景不是最优选择

### 3. 基数排序 (Radix Sort)

**算法原理：**
- 从最低位（个位）开始
- 对每一位使用稳定的计数排序
- 逐位排序直到最高位

**时间复杂度：** O(d × (n + k))，d 是位数，k 是基数（10）  
**空间复杂度：** O(n + k)  
**稳定性：** 稳定排序

**优点：**
- 对于固定位数的整数非常高效
- 稳定排序
- 不依赖比较操作

**缺点：**
- 需要额外空间
- 只适用于整数或可转换为整数的数据

## 编译和运行

### 编译程序

```bash
gcc -o age_sort_analysis age_sort_analysis.c -O2
```

### 运行程序

```bash
./age_sort_analysis
```

## 性能分析结果

程序会输出：
1. 每种算法排序前后的样本数据
2. 每种算法的执行时间
3. 排序结果的正确性验证
4. 详细的性能分析总结

## 结论

对于本问题（100万用户，年龄范围1-120岁）：

1. **计数排序是最优选择**
   - 时间复杂度接近 O(n)
   - 充分利用了年龄范围小的特点
   - 执行速度最快

2. **基数排序次之**
   - 性能优于快速排序
   - 稳定排序
   - 适合整数排序

3. **快速排序表现一般**
   - 虽然是经典算法
   - 但对于本场景不是最优选择
   - O(n log n) 的复杂度高于计数排序

## 算法选择建议

- **数据范围小且已知**：使用计数排序
- **需要稳定排序**：使用计数排序或基数排序
- **通用场景**：使用快速排序
- **数据量特别大**：考虑外部排序或分布式排序

## 技术要点

1. **内存管理**：使用 malloc/free 进行动态内存分配
2. **性能测量**：使用 clock() 函数测量执行时间
3. **结果验证**：实现了排序正确性验证函数
4. **代码复用**：通过函数指针实现测试框架

## 文件说明

- `age_sort_analysis.c` - 主程序源代码
- `年龄排序分析说明.md` - 本说明文档

## 扩展思考

1. 如果年龄范围扩大到 1-10000，哪种算法更优？
2. 如果需要按年龄和姓名双重排序，如何修改？
3. 如果数据量达到 1 亿，需要哪些优化？
4. 多线程排序能带来多少性能提升？
